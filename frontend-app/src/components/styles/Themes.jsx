import { createTheme } from '@mui/material/styles';

const customTheme = createTheme({
    palette: {
        primary: {
            main: '#b280357',
        },
        secondary: {
            main: '#ffb74d',
        },
        orange: {
            main: '#ffc570',
        },
    },
});

export default customTheme;
